# Python tooling for telemuze model conversion and native-NeMo benchmarks.
# Not needed for building or running the shipped Rust binaries.
#
# Setup:
#   uv venv .venv --python 3.12
#   uv pip install -r requirements.txt
#
# Only direct deps are pinned here; uv will resolve the rest of the NeMo
# transitive graph. Versions match the env used for the original conversions.

nemo-toolkit[asr] @ git+https://github.com/NVIDIA/NeMo.git@10e159746ffbfa5f0dfaa2a7851d210ffe27fd2a
torch==2.11.0
pytorch-lightning==2.6.1
lightning==2.4.0
transformers==4.57.6
huggingface_hub==0.36.2
hydra-core==1.3.2
omegaconf==2.3.0
onnx==1.21.0
onnxruntime==1.24.4
librosa==0.11.0
soundfile==0.13.1
numpy==2.4.4
